name: Slash Command (Cypress)
on:
  repository_dispatch:
    types: [cypress-command]
jobs:
  print:
    runs-on: ubuntu-20.04
    steps:
      - name: Print args 🖨️
        run: |
          echo 'machines ${{ github.event.client_payload.slash_command.args.named.machines }}'
          echo 'tags ${{ github.event.client_payload.slash_command.args.named.tags }}'
          echo 'PR ${{ toJson(github.event.client_payload.pull_request) }}'

  # cypress_tests:
  #   # https://github.com/bahmutov/cypress-workflows
  #   uses: bahmutov/cypress-workflows/.github/workflows/split.yml@v2
  #   with:
  #     nE2E: ${{ fromJson(github.event.client_payload.slash_command.args.named.machines) || 1 }}
  #     env: 'grepTags=${{ github.event.client_payload.slash_command.args.named.tags }}'
